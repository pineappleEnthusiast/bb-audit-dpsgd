name: bb_audit_dpsgd_arm64
channels:
  - pytorch
  - conda-forge
  - nvidia
  - defaults
dependencies:
  # Base dependencies
  - python=3.10
  - pip
  - numpy
  - scipy
  - matplotlib
  - pandas
  - jupyter
  - tqdm
  - pillow
  - pyyaml
  - h5py
  - requests
  - seaborn
  - scikit-learn
  
  # PyTorch with CUDA 12.1 support for ARM64 (latest stable as of 2024-09)
  - pytorch=2.3.0=py3.10_cuda12.1*  # This will use the ARM64 build
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
  
  # CUDA toolkit for ARM64
  - cudatoolkit=12.1
  - cudnn=8.9
  - nccl=2.19  # Latest stable version for CUDA 12.x
  
  # Additional ML/DL libraries
  - tensorboard
  - tensorboardx
  - torchinfo
  - torchmetrics
  
  # For data loading
  - numba
  - nvidia-nvtx
  - nvtx
  
  # For visualization
  - opencv
  - imageio
  - plotly
  
  # For development
  - ipykernel
  - ipywidgets
  - jupyter_contrib_nbextensions
  - black
  - flake8
  - mypy
  - pytest

  # Install pip packages
  - pip:
    - torch-privacymodel  # If needed for DP-SGD
    - opacus  # If using Opacus for differential privacy
    - tensorboard-plugin-profile  # For PyTorch profiler
    - torch-tb-profiler  # For PyTorch TensorBoard profiler plugin

# Environment variables for CUDA
variables:
  LD_LIBRARY_PATH: ${CONDA_PREFIX}/lib:${LD_LIBRARY_PATH}
  CUDA_HOME: ${CONDA_PREFIX}
  CUDA_PATH: ${CONDA_PREFIX}
  PATH: ${CONDA_PREFIX}/bin:${PATH}
